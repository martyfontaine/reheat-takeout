node_modules/
bun.lock
bun.lockb
*.log
.DS_Store

# Reheat runtime state (never commit a user's library index or logs)
/.work/
/work/
*.sqlite
*.sqlite-*
test/.tmp/

# Built app bundle — regenerated by `bun run build:app`, never committed
/dist/

# Walkthrough screenshot SOURCES + DaVinci Resolve audio-cache leftovers (img/).
# Dev-only inputs, not shipped or referenced by code — kept on disk, out of the repo
# (per HANDOFF notes; audited 2026-07-12).
/img/
*.pfl
