{
  "name": "reheat",
  "version": "0.1.0",
  "description": "macOS daemon that imports Google Takeout into Apple Photos — metadata-correct.",
  "keywords": [
    "google-takeout",
    "apple-photos",
    "google-photos",
    "exif",
    "metadata",
    "photos",
    "macos",
    "launchd",
    "bun",
    "photo-migration"
  ],
  "type": "module",
  "license": "MIT",
  "bin": {
    "reheat": "bin/reheat.ts"
  },
  "files": [
    "bin",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "reheat": "bun run bin/reheat.ts",
    "build:app": "bun run scripts/build-app.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
